{
  "name": "@knottasoft/teacher-assistant-mcp",
  "version": "1.1.0",
  "description": "MCP server for Teacher Assistant — FGOS lookup, FRP semantic search (FTS5+e5-base), DOCX export, grade analytics, academic hours calculator",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "teacher-assistant-mcp": "dist/index.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist/",
    "src/data/",
    "src/db/"
  ],
  "scripts": {
    "build": "tsc && npm run bundle",
    "bundle": "node esbuild.config.mjs",
    "build:db": "node ../scripts/build-fgos-db.mjs",
    "build:db:no-vec": "node ../scripts/build-fgos-db.mjs --no-vec",
    "start": "node dist/bundle.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@xenova/transformers": "^2.17.2",
    "better-sqlite3": "^11.10.0",
    "docx": "^9.1.0",
    "sqlite-vec": "^0.1.9",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/knottasoft/teacher-assistant.git",
    "directory": "mcp-server"
  },
  "keywords": [
    "mcp",
    "mcp-server",
    "claude",
    "teacher",
    "fgos",
    "education"
  ]
}
